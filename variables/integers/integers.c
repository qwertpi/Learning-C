#include <stdio.h>
void main (void)
{
 /* make an inetger called a */
 int a;
 /* make an integer called b and initalise it with a value of 3*/
 int b = 3;
 int c;
 /* give a a value of 2 */
 a = 2;
 /* give c the value of a plus b */
 c = a + b;
 /* %d acts as a placeholder for the vairbales which are speicified at the end of the string to be printed */
    
 printf ("The sum of adding %d and %d is %d\n", a, b, c);
 float a;
 c = a + b;
 printf ("The sum of adding %d and %d is %d\n", a, b, c);
}